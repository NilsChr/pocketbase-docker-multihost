{
  "name": "pocketbase-docker-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node index.js",
    "start": "cd output && docker compose down && docker compose build && docker compose up",
    "dev": "export NODE_ENV=development && node index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "yaml": "^2.1.3"
  }
}
