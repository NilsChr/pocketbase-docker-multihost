FROM alpine:3 as downloader

ARG TARGETOS
ARG TARGETARCH
ARG TARGETVARIANT
ARG VERSION

ENV BUILDX_ARCH="${TARGETOS:-linux}_${TARGETARCH:-amd64}${TARGETVARIANT}"

RUN wget https://github.com/pocketbase/pocketbase/releases/download/v${VERSION}/pocketbase_${VERSION}_${BUILDX_ARCH}.zip \
    && unzip pocketbase_${VERSION}_${BUILDX_ARCH}.zip \
    && chmod +x /pocketbase

# RUN wget https://github.com/pocketbase/pocketbase/releases/download/v${VERSION}/pocketbase_${VERSION}_${BUILDX_ARCH}.zip \
#    && unzip pocketbase_${VERSION}_${BUILDX_ARCH}.zip \
#    && chmod +x /pocketbase2

FROM alpine:3
RUN apk update && apk add ca-certificates && rm -rf /var/cache/apk/*

EXPOSE 8090
EXPOSE 8091

COPY first_process first_process
# COPY my_second_process my_second_process
COPY wrapper.sh wrapper.sh
CMD ./my_wrappewrapperr_script.sh

COPY --from=downloader /pocketbase2 /usr/local/bin/pocketbase2
# ENTRYPOINT ["/usr/local/bin/pocketbase", "serve", "--http=0.0.0.0:8090", "--dir=app1/pb_data", "--publicDir=app1/pb_public"]
